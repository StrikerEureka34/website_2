# Generated by krkn-docs-bot. Do not edit by hand.
source_repo: status
source_ref: HEAD
params:
- name: clusterJobs
  description: ClusterJobs contains the status of each cluster job
  type: array
  required: false
- name: clusterJobs[].cancelRequested
  description: CancelRequested indicates if the user has requested cancellation
  type: boolean
  required: false
- name: clusterJobs[].clusterApiUrl
  description: ClusterAPIURL is the API URL of the cluster for permission checks
  type: string
  required: false
- name: clusterJobs[].clusterName
  description: ClusterName is the name of the target cluster
  type: string
  required: true
- name: clusterJobs[].completionTime
  description: CompletionTime is when the job completed
  type: string
  required: false
- name: clusterJobs[].containerImage
  description: ContainerImage is the full container image path (registry/repository:tag) being run
  type: string
  required: false
- name: clusterJobs[].failureReason
  description: FailureReason contains a categorized failure reason (OOMKilled, ContainerError, etc.)
  type: string
  required: false
- name: clusterJobs[].jobId
  description: JobID is the unique identifier for this job
  type: string
  required: true
- name: clusterJobs[].lastRetryTime
  description: LastRetryTime is when the last retry was initiated
  type: string
  required: false
- name: clusterJobs[].maxRetries
  description: MaxRetries is the maximum number of retries allowed for this job
  type: integer
  required: false
- name: clusterJobs[].message
  description: Message contains additional information about the job status
  type: string
  required: false
- name: clusterJobs[].phase
  description: Phase is the current phase of the job (Pending, Running, Succeeded, Failed, Retrying, Cancelled, MaxRetriesExceeded)
  type: string
  possible_values:
  - Pending
  - Running
  - Succeeded
  - Failed
  - Retrying
  - Cancelled
  - MaxRetriesExceeded
  required: true
- name: clusterJobs[].podName
  description: PodName is the name of the pod running the scenario
  type: string
  required: false
- name: clusterJobs[].providerName
  description: ProviderName is the name of the provider that owns this cluster
  type: string
  required: true
- name: clusterJobs[].retryCount
  description: RetryCount is the number of times this job has been retried
  type: integer
  required: false
- name: clusterJobs[].startTime
  description: StartTime is when the job started
  type: string
  required: false
- name: conditions
  description: Conditions represent the latest available observations of the scenario run's state
  type: array
  required: false
- name: conditions[].lastTransitionTime
  description: 'lastTransitionTime is the last time the condition transitioned from one status to another.

    This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.'
  type: string
  required: true
- name: conditions[].message
  description: 'message is a human readable message indicating details about the transition.

    This may be an empty string.'
  type: string
  required: true
- name: conditions[].observedGeneration
  description: 'observedGeneration represents the .metadata.generation that the condition was set based upon.

    For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date

    with respect to the current state of the instance.'
  type: integer
  required: false
- name: conditions[].reason
  description: 'reason contains a programmatic identifier indicating the reason for the condition''s last transition.

    Producers of specific condition types may define expected values and meanings for this field,

    and whether the values are considered a guaranteed API.

    The value should be a CamelCase string.

    This field may not be empty.'
  type: string
  required: true
- name: conditions[].status
  description: status of the condition, one of True, False, Unknown.
  type: string
  possible_values:
  - 'True'
  - 'False'
  - Unknown
  required: true
- name: conditions[].type
  description: type of condition in CamelCase or in foo.example.com/CamelCase.
  type: string
  required: true
- name: failedJobs
  description: FailedJobs is the number of failed jobs
  type: integer
  required: false
- name: phase
  description: Phase is the overall phase of the scenario run
  type: string
  possible_values:
  - Pending
  - Running
  - Succeeded
  - PartiallyFailed
  - Failed
  required: false
- name: resiliencyScores
  description: 'ResiliencyScores contains per-cluster resiliency scores for this scenario run.

    Each entry represents the score calculated from the pod logs of a specific cluster.

    When a scenario runs on multiple clusters, this array will contain one entry per cluster.

    Populated only when the parent KrknGraphRun has Spec.ResiliencyScoreEnabled set to true.'
  type: array
  required: false
- name: resiliencyScores[].clusterName
  description: ClusterName is the name of the cluster this score applies to
  type: string
  required: true
- name: resiliencyScores[].score
  description: Score is the calculated resiliency score for this cluster (0-100)
  type: number
  required: true
- name: runningJobs
  description: RunningJobs is the number of currently running jobs
  type: integer
  required: false
- name: successfulJobs
  description: SuccessfulJobs is the number of successfully completed jobs
  type: integer
  required: false
- name: totalTargets
  description: TotalTargets is the total number of target clusters
  type: integer
  required: false
