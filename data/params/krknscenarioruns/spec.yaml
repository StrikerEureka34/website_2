# Generated by krkn-docs-bot. Do not edit by hand.
source_repo: spec
source_ref: HEAD
params:
- name: customRunName
  description: CustomRunName is a user-provided label for the run, displayed in the console
  type: string
  required: false
- name: environment
  description: Environment is a map of environment variables to set in the scenario pod
  type: object
  required: false
- name: files
  description: Files is a list of files to mount in the scenario pod
  type: array
  required: false
- name: files[].content
  description: Content is the base64-encoded content of the file
  type: string
  required: true
- name: files[].fileId
  description: FileID is the UUID of the source ConfigMap (optional, preserved for replay functionality)
  type: string
  required: false
- name: files[].mountPath
  description: MountPath is the absolute path where the file should be mounted
  type: string
  required: true
- name: files[].name
  description: Name is the name of the file
  type: string
  required: true
- name: kubeconfigPath
  description: KubeconfigPath is the path where kubeconfig will be mounted in the pod
  type: string
  default: /home/krkn/.kube/config
  required: false
- name: maxRetries
  description: MaxRetries is the maximum number of times to retry failed jobs
  type: integer
  default: '3'
  required: false
- name: ownerUserId
  description: OwnerUserID is the email address of the user who created this scenario run
  type: string
  required: false
- name: password
  description: Password is the password for registry authentication
  type: string
  secret: true
  required: false
- name: registryName
  description: RegistryName is the name of the saved private registry to use
  type: string
  required: false
- name: registryURL
  description: RegistryURL is the URL of the container registry
  type: string
  required: false
- name: retryBackoff
  description: RetryBackoff determines the backoff strategy for retries (exponential or fixed)
  type: string
  default: exponential
  possible_values:
  - exponential
  - fixed
  required: false
- name: retryDelay
  description: RetryDelay is the initial delay before retrying (e.g., "10s")
  type: string
  default: 10s
  required: false
- name: scenarioImage
  description: ScenarioImage is the container image for the scenario
  type: string
  required: true
- name: scenarioName
  description: ScenarioName is the name of the scenario to run
  type: string
  required: true
- name: scenarioRepository
  description: ScenarioRepository is the repository path in the registry
  type: string
  required: false
- name: targetClusters
  description: 'TargetClusters is a map of provider-name to list of cluster names

    Example: &#123;"krkn-operator": ["cluster1", "cluster2"], "krkn-operator-acm": ["cluster3"]}'
  type: object
  required: true
- name: targetRequestId
  description: TargetRequestID is the reference to the KrknTargetRequest CR
  type: string
  required: true
- name: token
  description: Token is the authentication token for the registry
  type: string
  secret: true
  required: false
- name: username
  description: Username is the username for registry authentication
  type: string
  required: false
