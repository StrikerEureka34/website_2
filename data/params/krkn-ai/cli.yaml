# Generated by krkn-docs-bot. Do not edit by hand.
source_repo: cli
source_ref: HEAD
params:
- name: run.kubeconfig
  description: Path to cluster kubeconfig file. Setting this will override value in config file.
  group: run
  type: string
  flag: kubeconfig
  required: false
- name: run.config
  description: Path to krkn-ai config file.
  group: run
  type: string
  flag: config
  required: false
- name: run.output
  description: Directory to save results.
  group: run
  type: string
  default: ./
  flag: output
  required: false
- name: run.format
  description: Format of the output file.
  group: run
  type: enum
  default: yaml
  flag: format
  possible_values:
  - json
  - yaml
  required: false
- name: run.runner-type
  description: Type of chaos engine to use.
  group: run
  type: enum
  flag: runner-type
  possible_values:
  - krknctl
  - krknhub
  required: false
- name: run.param
  description: Additional parameters for config file in key=value format.
  group: run
  type: string
  flag: param
  required: false
- name: run.seed
  description: Random seed for reproducible runs. Overrides seed in config file.
  group: run
  type: number
  flag: seed
  required: false
- name: run.verbose
  description: Increase verbosity of output.
  group: run
  type: number
  default: '0'
  flag: verbose
  required: false
- name: run.monitoring
  description: Launch live monitoring dashboard in the background.
  group: run
  type: boolean
  default: 'false'
  flag: monitoring
  required: false
- name: run.port
  description: Port to run Streamlit server on when monitoring is enabled.
  group: run
  type: number
  default: '8501'
  flag: port
  required: false
- name: run.allow-dangerous-scenarios
  description: Allow scenarios with a cluster-critical blast radius (e.g. service disruption).
  group: run
  type: boolean
  default: 'false'
  flag: allow-dangerous-scenarios
  required: false
- name: monitor.output
  description: Directory where results are saved.
  group: monitor
  type: string
  default: ./
  flag: output
  required: false
- name: monitor.port
  description: Port to run Streamlit server on.
  group: monitor
  type: number
  default: '8501'
  flag: port
  required: false
- name: discover.kubeconfig
  description: Path to cluster kubeconfig file.
  group: discover
  type: string
  flag: kubeconfig
  required: false
- name: discover.output
  description: Path to save config file.
  group: discover
  type: string
  default: ./krkn-ai.yaml
  flag: output
  required: false
- name: discover.namespace
  description: Namespace(s) to discover components in. Supports Regex and comma separated values.
  group: discover
  type: string
  default: .*
  flag: namespace
  required: false
- name: discover.pod-label
  description: Pod Label Keys(s) to filter. Supports Regex and comma separated values.
  group: discover
  type: string
  default: .*
  flag: pod-label
  required: false
- name: discover.node-label
  description: Node Label Keys(s) to filter. Supports Regex and comma separated values.
  group: discover
  type: string
  default: .*
  flag: node-label
  required: false
- name: discover.verbose
  description: Increase verbosity of output.
  group: discover
  type: number
  default: '0'
  flag: verbose
  required: false
- name: discover.skip-pod-name
  description: Pod name to skip. Supports comma separated values with regex.
  group: discover
  type: string
  flag: skip-pod-name
  required: false
- name: discover.save-strategy
  description: 'How to save: skip, overwrite (replace), or merge (add new components, keep your edits). Note: merge does not preserve comments.'
  group: discover
  type: enum
  default: skip
  flag: save-strategy
  possible_values:
  - skip
  - overwrite
  - merge
  required: false
- name: discover.learned-weights
  description: Path to a learned_weights.json from a previous run, to prioritize fitness queries.
  group: discover
  type: string
  flag: learned-weights
  required: false
