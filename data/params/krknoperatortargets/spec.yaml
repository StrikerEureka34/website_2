# Generated by krkn-docs-bot. Do not edit by hand.
source_repo: spec
source_ref: HEAD
params:
- name: caBundle
  description: 'CABundle is the base64-encoded CA certificate bundle for TLS verification

    Optional - if not provided and SecretType is not kubeconfig, TLS verification will be skipped'
  type: string
  required: false
- name: clusterAPIURL
  description: ClusterAPIURL is the Kubernetes API server URL
  type: string
  required: false
- name: clusterName
  description: ClusterName is the name of the target cluster
  type: string
  required: true
- name: insecureSkipTLSVerify
  description: 'InsecureSkipTLSVerify skips TLS certificate verification

    Only used when CABundle is not provided'
  type: boolean
  default: 'false'
  required: false
- name: secretType
  description: SecretType specifies the authentication method
  type: string
  possible_values:
  - kubeconfig
  - token
  - credentials
  required: true
- name: secretUUID
  description: SecretUUID is the UUID of the Secret containing the kubeconfig
  type: string
  required: true
- name: uuid
  description: UUID is the unique identifier for this target
  type: string
  required: true
