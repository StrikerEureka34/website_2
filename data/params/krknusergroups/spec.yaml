# Generated by krkn-docs-bot. Do not edit by hand.
source_repo: spec
source_ref: HEAD
params:
- name: clusterPermissions
  description: 'ClusterPermissions defines access permissions per cluster Key: cluster API URL (must match ClusterTarget.ClusterAPIURL from KrknTargetRequest) Value: set of allowed actions (view, run, cancel)'
  type: object
  required: true
- name: clusterPermissions.<key>.actions
  description: 'Actions is the list of allowed actions on this cluster Allowed values: "view" (can view cluster and scenario runs), "run" (can launch scenarios), "cancel" (can cancel running scenarios)'
  type: array
  possible_values:
  - view
  - run
  - cancel
  required: true
- name: description
  description: Description is a human-readable description of the group's purpose
  type: string
  required: false
- name: name
  description: Name is the group name (duplicates metadata.name for API convenience)
  type: string
  required: true
